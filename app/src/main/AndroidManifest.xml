<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.objeto_intent">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Objeto_Intent"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Declaración de Operacion2Activity (y otras actividades si las tienes) -->
        <activity android:name=".Operacion1Activity">
            <!-- Otros atributos específicos de la actividad -->
        </activity>
        <activity android:name=".Operacion2Activity">
            <!-- Otros atributos específicos de la actividad -->
        </activity>
        <activity android:name=".Operacion3Activity">
            <!-- Otros atributos específicos de la actividad -->
        </activity>
        <activity android:name=".Operacion4Activity">
            <!-- Otros atributos específicos de la actividad -->
        </activity>
        <activity android:name=".Operacion5Activity">
            <!-- Otros atributos específicos de la actividad -->
        </activity>

        <!-- Agrega aquí las declaraciones de otras actividades si las tienes -->

    </application>
</manifest>
